<?php
declare(strict_types=1);

/*
 * Global procedural helpers (env, config, view, abort, …).
 *
 * Populated during the Core-foundation extraction; kept present from the
 * first commit because composer.json autoloads this file.
 */
